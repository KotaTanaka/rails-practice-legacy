<div class="main reviews-index">
  <div class="container">
    <% @reviews.each do |review| %>
      <div class="reviews-index-item">
        <%= review.content %>
        <%= link_to("削除", "/reviews/#{review.id}/destroy", {method: "post"}) %>
      </div>
    <% end %>
  </div>
</div>
